services:
  - type: web
    name: imagingpedia-backend
    runtime: node
    plan: free
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
      - key: PORT
        value: 3000
    autoDeploy: true
